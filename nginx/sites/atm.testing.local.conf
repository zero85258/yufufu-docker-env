server {
    listen       80;
    server_name  atm.testing.local;

    index index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /code/atm.testing.local/;

    location / {
		index index.html;
    }
}
